# imageCaptioningWithCNNandRNN

In this task, the goal is to generate a caption for an image. This project will require a combination of CNN (for image feature extraction) and RNN (for caption generation).
